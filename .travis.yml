language: node_js
node_js:
  - "8"

branches:
  only:
  - "prototype-v0.7.0"

before_install:
  - npm install serverless -g

install:
  - make install

script:
  - echo 'No tests, this is a PROTOTYPE only'

after_success:
  - make deploy