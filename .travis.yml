language: node_js
node_js:
  - "8"
branches:
  only:
  - master
  - v0.10-alpha
  - /issue-*/
env:
  - NODE_ENV=development

before_install:
  - npm cache --force clean

script:
  - (cd web; npm install)
  - (cd web; npm test)
