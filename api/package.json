{
  "name": "pipeline-api",
  "version": "0.10.0",
  "description": "phase 1 - express - typescript - cucumber - serverless",
  "main": "server.js",
  "scripts": {
    "start": "tslint src/** && tsc --outDir dist src/** && node dist/server.js",
    "test": "tslint src/** && tsc --outDir dist src/** && cucumber-js ./tests/features --require ./tests --format ./node_modules/cucumber-pretty"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.5.0",
    "chai": "^4.1.2",
    "cucumber": "^4.2.1",
    "cucumber-pretty": "^1.4.4",
    "supertest": "^3.1.0",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"
  }
}
